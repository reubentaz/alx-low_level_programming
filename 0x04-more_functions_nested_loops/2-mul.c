#include "main.h"

/**
 * mul -multiply two integers
 * @x: the first number to multiply
 * @y:the second number to multiply the first
 * Return: the result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
